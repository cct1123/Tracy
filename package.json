{
  "name": "tracy-optical-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-native optical engineering workbench with sequential and Fresnel ray tracing.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist --port 4173",
    "test": "node --test",
    "lint": "eslint src scripts tests",
    "format": "prettier --write src scripts tests index.html package.json README.md docs eslint.config.js",
    "format:check": "prettier --check src scripts tests index.html package.json README.md docs eslint.config.js",
    "check": "npm run lint && npm test && npm run build && npm run format:check"
  },
  "dependencies": {
    "three": "0.128.0"
  },
  "devDependencies": {
    "eslint": "10.9.1",
    "prettier": "3.6.2"
  }
}
